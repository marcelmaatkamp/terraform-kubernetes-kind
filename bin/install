#!/bin/bash
application=${1:-apps}

terraform init -var-file="${application}.tfvars" &&\
terraform plan -var-file="${application}.tfvars" &&\
terraform apply -var-file="${application}.tfvars" --auto-approve
